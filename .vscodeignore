# Source & build inputs (only compiled out/ ships)
src/**
esbuild.mjs
tsconfig.json
**/*.ts
**/*.tsx
**/*.map

# Repo / agent infra — not part of the runtime
.claude/**
scripts/**
media/demo/**
mockups/**
CLAUDE.md
AGENTS.md
.git/**
.gitignore
.vscode/**
.vscode-test/**

# Anthropic Claude Agent SDK + its platform binaries — © Anthropic, All Rights Reserved (not ours to
# redistribute). Shape 2: provisioned at runtime from the official npm registry into globalStorage,
# never shipped in the vsix. (plans/Distributable)
node_modules/@anthropic-ai/claude-agent-sdk/**
node_modules/@anthropic-ai/claude-agent-sdk-*/**

# Dev probes / scratch — investigation artifacts, never shipped
probe-*
tmp-codex-schema/**

# Misc
*.vsix
**/.DS_Store
**/*.log
**/*vitest_out.txt
